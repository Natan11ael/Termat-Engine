{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch (CMake + MSVC)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\Debug\\main.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "preLaunchTask": "CMake: build"
        }
    ]
}